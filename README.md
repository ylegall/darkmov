# darkmov
toy markov text generator in D

Setup
--------
Required tools:
* D compiler
* DUB package manager

Compiling:
----------
1. To compile the command line program run:  
`dub build --build=release`

